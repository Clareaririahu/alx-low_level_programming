#include "main.h"

/**
* main - checks  if number is positive or negative
* Return: 0 if successful
*/

void postitive_or_negative(int i)
{
if (i < 0)
{
printf("%d is %s\n", i, "negative");
}
else if (i > 0)
{
printf("%d is %s\n", i, "poisitive");
}
else
}
printf("%d is %s\n", i, "zero");
}
return;
}
